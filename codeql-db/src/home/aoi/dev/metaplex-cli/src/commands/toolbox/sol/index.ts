import { Command } from '@oclif/core'

export default class ToolboxSol extends Command {

  static override description = 'SOL related tools such as balance, airdrop, etc.'

  public async run(): Promise<void> {

  }
}